name: Test_Workcell

config:
  redis_host: "solo_wei_redis"
  server_host: "solo_wei_server"
  server_port: 8000

#List of all components accessible in this workcell
modules:
  - name: solo
    model: solo
    interface: wei_rest_node
    config:
      rest_node_address: "http://solo:2000"
      rest_node_auth: ""
    workcell_coordinates: [0, 0, 0, 0, 0, 0]

locations: {}
